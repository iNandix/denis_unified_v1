apiVersion: v1
kind: ServiceAccount
metadata:
  name: denis-canary-sa
  labels:
    app: denis
    component: canary
---
# Optional: ClusterRole and ClusterRoleBinding for minimal permissions
# Only needed if you want to give the canary specific permissions
# For now, the ServiceAccount exists but has no special permissions
# (the canary only makes HTTP requests, no k8s API access needed)

# apiVersion: rbac.authorization.k8s.io/v1
# kind: ClusterRole
# metadata:
#   name: denis-canary-role
# rules: []
# ---
# apiVersion: rbac.authorization.k8s.io/v1
# kind: ClusterRoleBinding
# metadata:
#   name: denis-canary-binding
# roleRef:
#   apiGroup: rbac.authorization.k8s.io
#   kind: ClusterRole
#   name: denis-canary-role
# subjects:
# - kind: ServiceAccount
#   name: denis-canary-sa
#   namespace: default
